// Header.jsx

//Not being used due to side NAV
import React from 'react';

const Header = () => {
  return <header></header>;
}

export default Header; // Make sure to export the component as default
